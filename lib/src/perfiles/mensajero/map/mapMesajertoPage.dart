import 'package:flutter/material.dart';

class MapMessenger extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
     body: Center (child: 
     Text("localizacion del  meensajero"),
     
     ),
      
    );
  }
}